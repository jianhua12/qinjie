/**
 * 
 */

function goToRenshiRuzhishenqing() {
	mui.openWindow({
		url: 'renshi/ruzhishenqing/ruzhishenqing.html',
		id: 'ruzhishenqing',
		extras: { //这是要传的数据
		},
		waiting: {
			autoShow: false, //自动显示等待框，默认为true
		}
	});
}

function goToRenshiWaichudengji() {
	mui.openWindow({
		url: 'renshi/waichudengji/waichudengji.html',
		id: 'waichudengji',
		extras: { //这是要传的数据
		},
		waiting: {
			autoShow: false, //自动显示等待框，默认为true
		}
	});
}

function goToRenshiQingjiadengji() {
	mui.openWindow({
		url: 'renshi/qingjiadengji/qingjiadengji.html',
		id: 'qingjiadengji',
		extras: { //这是要传的数据
		},
		waiting: {
			autoShow: false, //自动显示等待框，默认为true
		}
	});
}

function goToRenshiShenpi() {
	mui.openWindow({
		url: 'renshi/shenpi/shenpi.html',
		id: 'renshishenpi',
		extras: { //这是要传的数据
		},
		waiting: {
			autoShow: false, //自动显示等待框，默认为true
		}
	});
}

function goToRenshiLizhishenqing() {
	mui.openWindow({
		url: 'renshi/lizhishenqing/lizhishenqing.html',
		id: 'lizhishenqing',
		extras: { //这是要传的数据
		},
		waiting: {
			autoShow: false, //自动显示等待框，默认为true
		}
	});
}

function goToXingzhengWuziguanli() {
	mui.openWindow({
		url: 'xingzheng/wuziguanli/wuziguanli.html',
		id: 'wuziguanli',
		extras: { //这是要传的数据
		},
		waiting: {
			autoShow: false, //自动显示等待框，默认为true
		}
	});
}

function goToXingzhengCaigoushenqing() {
	mui.openWindow({
		url: 'xingzheng/caigoushenqing/caigoushenqing.html',
		id: 'caigoushenqing',
		extras: { //这是要传的数据
		},
		waiting: {
			autoShow: false, //自动显示等待框，默认为true
		}
	});
}

function goToXingzhengShenpi() {
	mui.openWindow({
		url: 'xingzheng/shenpi/shenpi.html',
		id: 'xingzhengshenpi',
		extras: { //这是要传的数据
		},
		waiting: {
			autoShow: false, //自动显示等待框，默认为true
		}
	});
}

function goToCaizhengFeiyongshenqing() {
	mui.openWindow({
		url: 'caizhengguanli/feiyongshenqing/feiyongshenqing.html',
		id: 'feiyongshenqing',
		extras: { //这是要传的数据
		},
		waiting: {
			autoShow: false, //自动显示等待框，默认为true
		}
	});
}

function goToCaizhengShenpi() {
	mui.openWindow({
		url: 'caizhengguanli/shenpi/shenpi.html',
		id: 'caizhengguanlishenpi',
		extras: { //这是要传的数据
		},
		waiting: {
			autoShow: false, //自动显示等待框，默认为true
		}
	});
}
 