function back(){
	mui.back();
}


 
	
	
	
	
function goToShangbandaka(){
	mui.openWindow({
		url: 'shangbandaka/shangbandaka.html',
		id: 'shangbandaka',
		extras: { //这是要传的数据
		},
		waiting: {
			autoShow: false, //自动显示等待框，默认为true
		}
	});
}


function goToWaichudaka(){
	mui.openWindow({
		url: 'waichudaka/waichudaka.html',
		id: 'waichudaka',
		extras: { //这是要传的数据
		},
		waiting: {
			autoShow: false, //自动显示等待框，默认为true
		}
	});
}


function goToYijiandaka(){
	mui.openWindow({
		url: 'yijiandaka/yijiandaka.html',
		id: 'yijiandaka',
		extras: { //这是要传的数据
		},
		waiting: {
			autoShow: false, //自动显示等待框，默认为true
		}
	});
}

function goToWodedakajilu(){
	mui.openWindow({
		url: 'dakajilu/dakajilu.html',
		id: 'dakajilu',
		extras: { //这是要传的数据
		},
		waiting: {
			autoShow: false, //自动显示等待框，默认为true
		}
	});
}
